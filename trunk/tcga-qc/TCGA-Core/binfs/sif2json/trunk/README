sif2json.pl - Create a DCC-compliant "pending barcode" json object 
from a BCR Excel shipping information form

To install

perl Build.PL
(or perl Build.PL install_base=~ to install in user space)
./Build
./Build test
./Build install

If you are on a windows box you should use 'nmake' rather than 'make'.
