################################################################################
# Software License, Version 1.0 Copyright 2010 SRA International, Inc.
# Copyright Notice.  The software subject to this notice and license includes both human
# readable source code form and machine readable, binary, object code form (the "caBIG
# Software").
#
# Please refer to the complete License text for full details at the root of the project.
################################################################################

tcga.uuid.enableEmail=${enable.general.email}
tcga.uuid.mailHost=${mail.host}
tcga.uuid.defaultFromName=${email.from.name}
tcga.uuid.defaultFromAddress=${email.from.addr}
tcga.uuid.defaultCcAddress=${email.cc}
tcga.uuid.emailBCC=${email.bcc}
tcga.uuid.defaultReplyTo=${email.reply.to}
tcga.uuid.subjectPrefix=${subject.prefix}
tcga.uuid.failedmailto=${failed.email.to}
# Maximum number of uuids that are allowed to be created at one time from UI
tcga.uuid.maxAllowedUUIDs=${max.allowed.uuid}

# refresh of uuid browser procedure
tcga.uuid.uuidBrowserRefreshCronFrequencyTimer=${refresh.cron.uuid}

# LDAP settings
ldap_authentication_server_url=${ldap.authentication.server.url}
ldap_user_search_base=${ldap.user.search.base}
ldap_user_search_filter=${ldap.user.search.filter}

